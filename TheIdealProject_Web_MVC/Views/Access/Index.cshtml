@model IEnumerable<TheIdealProject_Web_MVC.Models.Tm_User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutExternal.cshtml";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.ActionLink("Login", "Login", new { id = item.TmUserPk }) |
            @Html.ActionLink("Edit", "Edit", new { id = item.TmUserPk }) |
            @Html.ActionLink("Details", "Details", new { id = item.TmUserPk }) |
            @*@Html.ActionLink("Delete", "Delete", new { id=item.TmUserPk })*@
        </td>
    </tr>
}

</table>
