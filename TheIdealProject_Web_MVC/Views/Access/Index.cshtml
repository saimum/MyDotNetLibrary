@model IEnumerable<TheIdealProject_Web_MVC.Models.TM_User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutExternal.cshtml";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.User_Name)
        </td>
        <td>
            @Html.ActionLink("Login", "Login", new { id = item.TM_UserPk }) |
            @Html.ActionLink("Edit", "Edit", new { id = item.TM_UserPk }) |
            @Html.ActionLink("Details", "Details", new { id = item.TM_UserPk }) |
            @*@Html.ActionLink("Delete", "Delete", new { id=item.TM_UserPk })*@
        </td>
    </tr>
}

</table>
